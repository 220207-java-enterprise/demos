// With ES6 default imports, the name declared here is the variable 
// name that holds the exported value
import login from './components/login/login.js';

window.onload = function() {
    login.render();
    login.render();
}